# M137 — Composable Tool Calling: Progressive Disclosure, Inner Thinking, Advisor Ordering

- **Milestone:** M137
- **REQ:** REQ-134
- **DEC:** DEC-007, DEC-012
- **Status:** Active
- **Date:** 2026-06-24

## Current Focus

Apply three improvements from Spring AI 2.0's composable tool calling: (1) `ToolSearchToolCallingAdvisor` for progressive tool disclosure, (2) `AugmentedToolCallbackProvider` for inner-thinking traceability, (3) move `SessionMemoryAdvisor` inside the tool loop.

## Tasks

1. [ ] Create `AgentThinking` record in `llm/domain/`.
2. [ ] Replace `ToolCallingAdvisor` with `ToolSearchToolCallingAdvisor` in `MedicalAgentConfiguration`.
3. [ ] Add tool-search-advisor properties to `application.yml`.
4. [ ] Wrap tool objects with `AugmentedToolCallbackProvider<AgentThinking>` in both config classes.
5. [ ] Add `.order(ToolCallingAdvisor.DEFAULT_ORDER + 1)` to `SessionMemoryAdvisor`.
6. [ ] Update tests for new advisor wiring.
7. [ ] Run `mvn verify`, fix failures.
8. [ ] Security review.
9. [ ] Run `sync-memory-index.sh --check`.
10. [ ] Commit + merge to develop.

## Open Questions

- None.

## Risks

- RISK-138: Vector store availability — fall back to `regex` index.
- RISK-139: Inner thinking token cost — monitor via telemetry.
- RISK-140: Session storage growth — compaction already configured.
